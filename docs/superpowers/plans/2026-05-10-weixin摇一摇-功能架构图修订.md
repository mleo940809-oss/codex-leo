# Weixin摇一摇 功能架构图修订 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将功能架构图修订为与 `Weixin摇一摇_PRD_v1.4_20260510.md` 完全一致的最终可冻结版本。

**Architecture:** 保持原有三层/两层能力划分不变，仅修正状态术语与边界注释，避免引入新的职责或流程。修改聚焦于图内文案、状态标签和主源注释，不改变整体布局。

**Tech Stack:** 图像编辑

---

### Task 1: 状态术语修正

**Files:**
- Modify: `E:\Codex\New_rules\docs\versions\Weixin摇一摇\Weixin摇一摇_功能架构图_v1.4_20260510.png`

- [ ] **Step 1: Replace the generic batch status label**

将 `状态管理` 替换为 `批次状态管理`。

- [ ] **Step 2: Add batch status source annotation**

在 `批次管理` 区块下补充小标签 `批次状态主源`。

- [ ] **Step 3: Add activity status source annotation**

在 `活动管理` 区块下补充小标签 `活动状态主源`。

### Task 2: 图例与术语收敛

**Files:**
- Modify: `E:\Codex\New_rules\docs\versions\Weixin摇一摇\Weixin摇一摇_功能架构图_v1.4_20260510.png`

- [ ] **Step 1: Preserve page split and capability split**

保持 `活动管理` 与 `批次管理` 的主分支不变，只调整术语。

- [ ] **Step 2: Verify no remaining ambiguous status terms**

检查图中是否仍存在 `状态管理` 的泛化写法，如有则全部替换。

### Task 3: Visual Verification

**Files:**
- Verify: `E:\Codex\New_rules\docs\versions\Weixin摇一摇\Weixin摇一摇_功能架构图_v1.4_20260510.png`

- [ ] **Step 1: Validate against PRD v1.4**

确认图中状态词、职责边界、页面拆分与 PRD 一致。

- [ ] **Step 2: Confirm readability**

确认新增标签不遮挡主体内容，图面仍保持清晰可读。

