# AGENTS.md for `E:\Codex\New_rules`

## 适用范围

本规则只适用于本工作区内的**文档评审类任务**，包括但不限于：

- 产品需求管理流程规范
- PRD、MRD、BRD、SOP、流程规范、制度规范
- 需求评审文档、方案评审文档、复盘文档、说明文档
- 与“是否合理、是否完整、是否可执行、是否可测试、是否有歧义”相关的文档审查

本规则**不适用于**以下任务：

- 纯代码实现、调试、重构、性能优化
- 纯 UI 设计、视觉稿生成、交互稿制作
- 单纯的文字润色、格式整理、翻译
- 与“评审”无关的日常问答
- 需要立即执行的产品开发计划，且不涉及文档审查结论

如果一个任务同时包含文档评审和其他工作，应先按本规则完成文档评审，再进入后续非评审工作。

## 规则优先级

1. 先遵守用户在当前对话中明确提出的要求。
2. 再遵守本 `AGENTS.md`。
3. 再遵守更具体的子目录规则文件。
4. 最后再使用通用技能和默认工作流。

如果不同规则发生冲突，以**更具体、更接近当前任务**的规则为准；若仍冲突，停止并向用户确认。

## 文档评审固定组合

当任务属于“文档评审”时，必须默认采用以下固定组合：

1. 主审规则：`superpowers:receiving-code-review`
2. 并行交叉评审：`superpowers:dispatching-parallel-agents`
3. 文档落地：`documents`
4. 若评审对象明显属于 PRD 或 PRD 流程相关文档，额外启用 `prd-max`

## PRD 输出模板强制规则

当任务属于 PRD 产出、PRD 改写、PRD 模板整理、PRD 流程复用或需求交付材料生成时，必须强制使用以下 Markdown 模板作为唯一输出结构来源：

- `E:\Codex\New_rules\docs\PRD工作流复用模板.md`

强制要求：

- 输出 PRD 时，章节顺序、标题层级、表格字段和填写建议必须以该模板为准。
- 不得擅自改用其他 PRD 模板结构，除非用户明确要求切换模板。
- 如果需要生成 DOCX，则 DOCX 必须由该 MD 模板内容转换而来，并保持字段语义一致。
- 如果模板与当前需求存在冲突，优先在模板内补充说明，不得静默删除模板中要求的核心章节。

## 工作流总入口与产物链路

在处理工作流治理、文档版本、产物留档或交付链路相关任务时，先参考以下入口文档，但不得把它们当成新的规则源：

- `[docs/工作流总入口.md](/E:/Codex/New_rules/docs/工作流总入口.md)`：仅作导航，不参与规则优先级仲裁。
- `[docs/docx产物链路说明.md](/E:/Codex/New_rules/docs/docx产物链路说明.md)`：仅说明 `.docx` 产物链路，不定义正文规则。

`.docx` 的正式绑定记录只以 `docs/docx/<文档名>/binding.md` 为唯一正式载体，不得同时以多个正式载体并行定义。

## 执行要求

### 1. 主审规则

所有文档评审必须先按 `superpowers:receiving-code-review` 的方式进行技术审查：

- 先验证，再判断
- 不做表面认同
- 优先发现遗漏、矛盾、歧义、不可执行点、不可测试点

### 2. 并行交叉评审

当文档评审需要多角度审查时，必须使用 `superpowers:dispatching-parallel-agents` 拆分视角并行处理。

默认推荐视角如下：

- 产品视角
- 研发视角
- 测试视角
- 流程/运营视角
- 风控/合规视角

如果任务规模较小，不必强行启用全部角色，但至少应保留两个独立视角进行交叉审查。

### 3. 文档落地

评审结论必须尽量落到实际文档中，优先使用 `documents` 能力处理：

- 批注
- 红线修改
- 结构修订
- 格式修正
- 评审意见归档

### 4. PRD 专项加严

如果评审对象是 PRD、PRD 流程规范，或明显以产品需求为核心的文档，则必须额外启用 `prd-max`，并按其要求检查：

- 需求完整性
- 可执行性
- 可测试性
- 可追踪性
- 审阅准备度

## 何时视为“文档评审”

满足以下任一条件，即视为文档评审并启用本规则：

- 用户明确要求“评审”“审查”“review”“交叉评审”“多角色评审”
- 任务目标是找出文档缺陷、遗漏、歧义、冲突或风险
- 任务目标是判断文档是否足够进入下一阶段
- 任务目标是对规范、流程、制度、PRD、SOP 进行质量检查

## 何时不要套用本规则

以下情况不要自动套用本规则：

- 用户只是要你总结内容，而不是评审
- 用户只是要你改写、润色、翻译文本
- 用户只是要你生成新文档草稿，且没有“评审”目标
- 用户只是要你执行代码、修 bug、做界面、跑测试

## 处理原则

- 文档评审优先看问题，不优先看修辞。
- 发现规则冲突时，先停，再澄清。
- 评审结论必须可落地，不能只停留在口头建议。
- PRD 类文档默认采用更严格标准，不得降级为普通文案检查。

## 会话记忆强制规则

当任务涉及跨会话延续、上下文压缩风险、后续接续阅读或长期协作时，必须执行 `docs/会话记忆强制执行规则.md` 中的要求：

1. 核心强制
   - 禁止只在会话结束时才总结
   - 一旦出现压缩前兆，必须先总结再继续
   - 每个会话必须对应单一记录，不得拆散为多个孤立片段
   - 关键结论、决定、待办、风险和未决问题必须持续写入记录
   - 每次执行会话总结时，都必须产出对应的会话记录文档，不得只停留在聊天总结
   - 执行这类会话记忆沉淀工作时，必须优先使用 `notion-knowledge-capture`
   - 当需要定义、变更或强化这类行为规则时，必须先按 `superpowers:brainstorming` 的流程完成设计确认，再进入落地
2. 默认执行
   - 新开会话时，应先读取总索引和相关历史记录，再进入后续处理
3. 条件强制
   - 当任务涉及跨会话延续、决策型问题、上下文冲突、长任务接续或明显压缩风险时，必须先完成历史读取，再给出结论、建议、判断或方案
