# AGENTS.md for `E:\Codex\New_rules`

## 适用范围

本文件是当前工作区的唯一 AI 执行入口，只定义入口、优先级和强制读取规则；具体流程、门禁、产物、提交、原型、`.docx` 和会话记忆规则统一以 [`docs/AI工作流统一执行指引.md`](/E:/Codex/New_rules/docs/AI工作流统一执行指引.md) 为准。

## 规则优先级

1. 用户在当前对话中明确提出的要求。
2. 本 `AGENTS.md`。
3. [`docs/AI工作流统一执行指引.md`](/E:/Codex/New_rules/docs/AI工作流统一执行指引.md)。
4. PRD 模板与专项产物模板。
5. 通用技能、工具说明和默认行为。

若发生冲突，优先采用更接近当前任务、且更具体的规则；若仍无法消解，先停止并向用户确认。

## 必读入口

处理任何工作前，必须先读取：

1. 本 `AGENTS.md`。
2. [`docs/AI工作流统一执行指引.md`](/E:/Codex/New_rules/docs/AI工作流统一执行指引.md)。
3. 当前任务需要的模板文件或产物文件。

不再把多个旧规范文件作为并列入口使用。旧文件如已归档，只能作为历史参考，不参与当前规则优先级仲裁。

## 任务分类

先判断当前任务属于哪一类，只能选一条主路径，不要混跑：

- 文档评审
- PRD 产出或 PRD 改写
- 产品需求管理
- 原型绑定或原型归档
- `.docx` 交付
- Git 提交或版本留档
- 会话记忆沉淀
- 其他普通任务

## 强制执行原则

- 先确认任务类型，再判断阶段。
- 先选唯一主路径，再读取对应规则。
- 先完成门禁，再进入下一阶段。
- 先验证，再宣称完成。
- 不能把草稿当正式交付。
- 不能把前期 PRD 和正式 PRD 混用同一文件反复覆盖。
- 不能跳过版本、绑定、归档或确认。
- 结果必须落到可追踪文档或实际产物中。

## PRD 强制规则

PRD 相关任务必须遵守：

- 前期探讨、问题定义、范围收敛、方案草案：使用 [`docs/PRD工作流复用模板.md`](/E:/Codex/New_rules/docs/PRD工作流复用模板.md)。
- 正式评审、需求冻结、研发交付、测试验收、最终归档：使用 [`docs/standard-prd-template.md`](/E:/Codex/New_rules/docs/standard-prd-template.md)。
- 进入正式阶段必须切换到正式模板。
- PRD、原型、交付包必须对齐，缺少门禁项不得按正式交付推进。

## 历史规则

旧版分散规则文件已被整合进统一指引。若需要追溯旧口径，只能在历史归档目录中查阅，不得把历史文件作为当前执行入口。
